<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"  
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
            xmlns:vm="clr-namespace:ICS_Project.App.ViewModels.MusicTrack"
            xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
            x:Class="ICS_Project.App.Views.MusicTrack.Popups.MusicTrackDetailView"
            x:DataType="vm:MusicTrackDetailViewModel"
            Color="Transparent">

    <Frame
        BackgroundColor="{StaticResource Gradient2}"
        BorderColor="{StaticResource Tertiary}"
        CornerRadius="30"
        >
        
        <Grid ColumnDefinitions="auto, *"
              RowDefinitions="Auto, auto, auto, auto, auto, auto"
              ColumnSpacing="10"
              RowSpacing="5"
        >

            <Frame
             Grid.Column="0"
             Grid.Row="0"
             Grid.RowSpan="5"
             CornerRadius="30"
             IsClippedToBounds="True"
             BorderColor="{StaticResource Tertiary}"
             Padding="0"
             WidthRequest="180"
             HeightRequest="180"
             Margin="10,10,5,5">

                <Image  
                    Source="redhotchilli.png" 
                    Aspect="AspectFill"
                    WidthRequest="180"
                    HeightRequest="180" />
            </Frame>

            <Frame
                 Grid.Column="1"
                 Grid.Row="0"
                 Grid.RowSpan="5"
                 CornerRadius="30"
                 BorderColor="{StaticResource Tertiary}"
                 BackgroundColor="{StaticResource SecondaryCardBackgroundColor}"
                 Padding="10"
                 Margin="0,10,5,5">
                <VerticalStackLayout Spacing="5">
                    <Label
                FontSize="25"
                FontAttributes="Bold"
                TextColor="{StaticResource PrimaryTextColor}"
                Text="{Binding TrackDetails.Title}">
                    </Label>

                    <Label
                FontSize="12"
                FontAttributes="Bold"
                TextColor="{StaticResource PrimaryTextColor}"
                Text="Authors">
                    </Label>

                    <Label
                 FontSize="12"
                 FontAttributes="Bold"
                 TextColor="{StaticResource PrimaryTextColor}"
                 Text="Album: {Binding TestMusicTrack.Album}">
                    </Label>

                    <Label
                        FontSize="12"
                        FontAttributes="Bold"
                        TextColor="{StaticResource PrimaryTextColor}"
                        Text="Genre{Binding TestMusicTrack.Genres}">
                    </Label>

                    <Label
                        FontSize="12"
                        FontAttributes="Bold"
                        TextColor="{StaticResource PrimaryTextColor}"
                        Text="{Binding TrackDetails.Length, StringFormat='Length: {0:mm\\:ss}'}">
                    </Label>
                </VerticalStackLayout>
            </Frame>


            <Frame
                Grid.Column="0" Grid.ColumnSpan="2"
                Grid.Row="5"
                CornerRadius="15"
                BorderColor="{StaticResource Tertiary}"
                BackgroundColor="{StaticResource CardBackgroundColor}"
                Padding="10"
                Margin="10, 5, 5, 5">
                <Label
                   FontSize="12"
                   FontAttributes="Bold"
                   TextColor="White"
                   Text="{Binding TrackDetails.Description}">
                </Label>
            </Frame>

            <Button
                 Grid.Row="0" Grid.Column="1"
                 CornerRadius="16"
                 WidthRequest="40"
                 HeightRequest="40"
                 VerticalOptions="Start"
                 HorizontalOptions="End"
                 BorderWidth="2"
                 TextColor="{StaticResource PrimaryTextColor}"
                 BackgroundColor="{StaticResource PrimaryButtonColor}"
                 Margin="0, 15, 15, 10"
                 Padding="1"
                 FontSize="15"
                 Text="···">
            </Button>

        </Grid>
    </Frame>


</toolkit:Popup>